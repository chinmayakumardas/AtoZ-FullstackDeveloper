let str=" chinmaya kumar das ";

console.log(
    
    str.charAt(2),
    str.charCodeAt(2),
    str.at(-1),
    str[1],
    str.repeat(2),
    str.replace('a','A'),
    str.replaceAll('a','A'),
    str.split(" "),
    str.concat('i er very bad gut'),
    str.toLowerCase(),
    str.toUpperCase(),
    str.split(" ").join(),
    str.startsWith('a'),
    str.endsWith(" "),
    `${str} is good boy`,

    str.substring(0,15),
    str.substr(0,5),
    str.includes('s'),
    str.slice(0,5),
    str.indexOf('c')
    ,str.lastIndexOf('a'),
    str.match('c'),
    str.matchAll('a')
,str.trimEnd(),
str.trimEnd(),
    str.trim(),

    '2'.padStart(4,'0'),
    '4'.padEnd(2,'x'),
    str.search('a')

)


